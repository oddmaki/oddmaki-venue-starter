# Venue Configuration
NEXT_PUBLIC_VENUE_ID=
NEXT_PUBLIC_VENUE_NAME=

# Chain Configuration (defaults to Base mainnet 8453 if unset)
#NEXT_PUBLIC_CHAIN_ID=8453   # Base mainnet
#NEXT_PUBLIC_CHAIN_ID=84532  # Base Sepolia
NEXT_PUBLIC_CHAIN_ID=

# The Graph gateway API key.
# Optional locally (SDK falls back to a free, rate-limited Studio endpoint).
# Set this in your production environment for the decentralized gateway.
# Get one for free: https://thegraph.com/studio → API Keys → Create API Key
# Add your production domain(s) under the key's Authorized Domains and set a
# monthly spending cap before deploying. See README → "Subgraph data".
NEXT_PUBLIC_GRAPH_API_KEY=

# Auth: RainbowKit (default, crypto-native, free) or Privy (consumer-friendly,
# free tier then paid).
NEXT_PUBLIC_AUTH_PROVIDER=rainbowkit

# Privy (uncomment both lines to switch)
#NEXT_PUBLIC_AUTH_PROVIDER=privy
#NEXT_PUBLIC_PRIVY_APP_ID=your-app-id

# IPFS — required only if you enable image uploads in the create-market flow.
# Leave PINATA_JWT empty/commented to disable uploads cleanly; setting it to a
# placeholder will pass the route's guard and surface a confusing Pinata error.
#PINATA_JWT=
NEXT_PUBLIC_IPFS_GATEWAY="https://gateway.pinata.cloud/ipfs/"

# Theme editor at /admin/theme — disable before production.
NEXT_PUBLIC_ENABLE_THEME_EDITOR=false
